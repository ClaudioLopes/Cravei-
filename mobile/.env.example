# URL base da API. Em emulador/web use localhost; num celular físico, use o IP da sua
# máquina na rede local (ex.: http://192.168.0.10:3000), já que "localhost" no celular
# apontaria para o próprio celular.
EXPO_PUBLIC_API_URL=http://localhost:3000
